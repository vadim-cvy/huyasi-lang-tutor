import { SizesNavigator } from '../../_internal/sizing/utils/SizesNavigator';
import { whitespaceSizes } from '../data/whitespaceSizes';

export const whitespaceSizesNavigator = new SizesNavigator(whitespaceSizes);
