import { SessionTtlAbsoluteHandler } from './session-ttl-absolute-handler';

describe('SessionTtlAbsoluteHandler', () => {
  it('should be defined', () => {
    expect(new SessionTtlAbsoluteHandler()).toBeDefined();
  });
});
